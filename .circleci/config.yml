version: 2.1

#########
# Common
#########
orbs:
  python: circleci/python@1.4.0

##################
# Job Definitions
##################
jobs:
  build:
    docker:
      - image: cimg/python:3.10.0
    steps:
      - checkout
      - run: python --version
      - run: ifconfig
      - run: scp -r -o StrictHostKeyChecking=no ./ ubuntu@10.123.0.88:/home/ubuntu

############
# Workflows
############
workflows:
  sample: 
    jobs:
      - build
